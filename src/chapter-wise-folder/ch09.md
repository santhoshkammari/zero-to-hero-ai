# Chapter 09 — CNNs & Computer Vision

## Status: ✅ v3 COMPLETE — Variable-depth, concept-driven, conversational tone

## Consolidation: 14 → 9 sections
- s01+s02+s03 merged → "CNN Building Blocks" (convolution, pooling, feature hierarchies, receptive field)
- s04 → "Architecture Evolution" (LeNet→AlexNet→VGG→ResNet→EfficientNet→ViT)
- s05 → "Transfer Learning" (the practical skill — never train from scratch)
- s06 → "Image Augmentation" (geometric, color, cutout, mixup, RandAugment)
- s07 → "Object Detection" (YOLO, Faster R-CNN, DETR, anchors, NMS)
- s08 → "Segmentation" (semantic, instance, panoptic, U-Net, SAM)
- s09+s10 merged → "Modern Vision" (CLIP, Stable Diffusion, multimodal LLMs)
- s11 → "Interpretability & Visualization" (Grad-CAM, saliency, debugging)
- s12+s13+s14 → "Nice to Know" (video, pose, visual search, NeRF, optical flow, MOT, super-res)

## Sections & Depth
| # | Section | Depth | Key Content |
|---|---------|-------|-------------|
| s01 | CNN Building Blocks | DEEP | Convolution intuition (pattern detector), stride/padding/dilation, depthwise separable, transposed conv, pooling types, GAP, feature hierarchies, receptive field |
| s02 | Architecture Evolution | DEEP | LeNet→AlexNet→VGG→Inception→ResNet (skip connections, DEEP)→DenseNet→MobileNet→EfficientNet→ViT (DEEP)→Swin→ConvNeXt, comparison table |
| s03 | Transfer Learning | DEEP | Feature extraction, fine-tuning strategies (gradual unfreezing, discriminative LR), decision table by data size/domain, freeze BN, timm/torchvision hubs, negative transfer, knowledge distillation |
| s04 | Image Augmentation | Moderate | Geometric, color, Cutout/MixUp/CutMix/Mosaic, decision table, RandAugment, Albumentations pipeline, TTA |
| s05 | Object Detection | Moderate | IoU, anchors, NMS, R-CNN→Faster R-CNN, YOLO evolution, SSD, RetinaNet/focal loss, DETR, anchor-free, comparison table, mAP |
| s06 | Segmentation | Moderate | Semantic/instance/panoptic, FCN, U-Net (skip connections), DeepLab/ASPP, Mask R-CNN, SAM, Dice loss, mIoU |
| s07 | Modern Vision | Moderate | CLIP (contrastive, zero-shot), DINOv2, latent diffusion/Stable Diffusion, GAN retrospective, multimodal LLMs (GPT-4V, LLaVA) |
| s08 | Interpretability & Visualization | Brief | Grad-CAM, saliency maps, attention vis, occlusion, debugging workflow |
| s09 | Nice to Know | Brief | Video (C3D, TimeSformer), pose estimation, visual search/FAISS, NeRF, optical flow, MOT, super-resolution, deformable conv, SE blocks |

## Quality Bar
Reader understands:
- How convolutions detect features (pattern detector that slides)
- Architecture evolution and WHY each step mattered (degradation→skip connections, patches→ViT)
- Transfer learning as the default approach (never train from scratch)
- Modern vision awareness (ViT, CLIP, diffusion models, multimodal LLMs)
